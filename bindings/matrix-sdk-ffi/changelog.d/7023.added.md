Add `Client::notification_client_with_timeouts`, which takes a
`NotificationTimeouts` record, so that clients with a larger time budget can
raise the timeouts applied while fetching notifications.
`Client::notification_client` is unchanged and keeps using the defaults.

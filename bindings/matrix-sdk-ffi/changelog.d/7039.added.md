Expose the send state of our pending edits and redactions on
`EventTimelineItem` and of our pending reactions on `ReactionSenderData`, and
add `Timeline::retry_send()` / `Timeline::abort_send()` with `SendTarget` to
retry or abort them.

[**breaking**] `Client::enable_automatic_backpagination` has been removed. Automatic back-pagination is now enabled via `ClientBuilder::enable_automatic_back_pagination(bool)`, set before the client is built.

Add `Client::run_search_backfill(SearchBackfillStrategy)` which starts a 
background sweep that back-paginates message history for every room (down to ~3 
months, front-loaded by recency) to populate the search index.

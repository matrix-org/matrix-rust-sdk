`Client::is_livekit_rtc_supported` is not doing fallbacks to the well-known discovery anymore,
and instead relies on the new `Client::discover_rtc_transports` that respects the
`Client::disable_well_known_lookup` flag.

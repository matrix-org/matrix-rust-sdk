Add `ClientBuilder::discover_server_support`, returning the contacts and support
page a server advertises at `/.well-known/matrix/support`, or `None` when it
advertises nothing or cannot be reached. It stays callable once `build` has
failed, so a client can name the administrator in the error it shows when a
homeserver is unreachable.

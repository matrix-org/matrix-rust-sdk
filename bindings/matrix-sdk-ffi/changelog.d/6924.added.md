Add `discover_server_support`, returning the contacts and support page a server
advertises at `/.well-known/matrix/support`, or `None` when it advertises
nothing or cannot be reached. This lets a client name the administrator in the
error it shows when a homeserver is unreachable.

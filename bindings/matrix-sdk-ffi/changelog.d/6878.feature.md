Add `SqliteStoreBuilder::high_entropy_passphrase`, a faster alternative to
`SqliteStoreBuilder::passphrase` for randomly generated passphrases. This
setting migrates the passphrase to a key-based setup.

Do NOT use it with human-chosen passphrases as they would lose their brute-force
protection.

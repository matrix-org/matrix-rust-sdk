Added `Timeline::editImage`, `editVideo`, `editAudio` and `editFile`, which replace the attachment of a message
the current user sent, or add one to a message which had none. The new attachment is uploaded through the send queue,
then an edit carrying it is sent.

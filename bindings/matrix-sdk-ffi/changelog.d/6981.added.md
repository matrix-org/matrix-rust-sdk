Add `Client::sendEncryptedToDeviceMessage` and `SendToDeviceOutcome` to Olm-encrypt a custom to-device message and send it
to a set of recipient devices, reporting the devices that could not be reached.

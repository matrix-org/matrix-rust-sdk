Add `Client::sendEncryptedToDeviceMessage` and `SendToDeviceResult` to Olm-encrypt a custom to-device message and send it
to a set of recipient devices, reporting the devices that could not be reached.

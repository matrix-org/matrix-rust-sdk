Fixed a potential deadlock in `SessionVerificationController` where delegate callbacks were invoked while the delegate `RwLock` read guard was held, so a delegate that detached itself via `set_delegate` would deadlock.

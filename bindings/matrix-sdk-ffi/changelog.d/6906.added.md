Expose `RoomListService::unsubscribe_to_rooms` and
`RoomListService::clear_and_subscribe_to_rooms`, so consumers can release room
subscriptions they no longer need, or replace the whole subscription set.

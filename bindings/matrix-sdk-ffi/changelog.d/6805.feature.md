Expose `SqliteStoreBuilder::key` to allow clients to specify a key as a byte array that will be used as is, skipping the key derivation process and speeding up opening DB connections. Note this should only be used for values with high entropy, for user-provided passphrases the `SqliteStoreBuilder::passphrase` function should still be used.

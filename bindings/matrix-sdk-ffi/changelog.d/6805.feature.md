Add `Client::run_search_backfill(BackPaginationStrategy)`, which starts a background sweep that back-paginates message history for every room (down to ~3 months, front-loaded by recency) to populate the search index. It returns a `TaskHandle` to stop the sweep (e.g. from a background-task expiration handler); progress lives in the store, so a later run resumes automatically.

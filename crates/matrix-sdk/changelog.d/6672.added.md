Add client-owned sync presence configuration and an explicit presence update
API. Generated sync requests use the client sync presence by default, while
classic sync settings and low-level sliding sync instances can still override it.

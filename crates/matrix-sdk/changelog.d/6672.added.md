Allow configuring the presence state sent with sliding sync requests. This lets
clients perform sliding sync with `offline` or `unavailable` presence instead
of implicitly using the default online presence.

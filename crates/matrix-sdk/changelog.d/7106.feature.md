Allow sending events as sticky as per [MSC4354](https://github.com/matrix-org/matrix-spec-proposals/pull/4354). Stickiness is enabled via `SendMessageLikeEvent::with_sticky_duration`. Whether or not the homeserver actually supports sticky events can be checked with `Client::supports_sticky_events`.

Added `MediaFetcher::get_media_content_with_config`. With this, you can retrieve the content of some `MediaSource`
providing a custom `RequestConfig` to handle retries or timeouts. Also implemented it for `DefaultMediaFetcher`.
Add `discover_server_support`, which reads the ways a server's administrator can
be reached from the `/.well-known/matrix/support` file that server advertises.
It needs neither a `Client` nor a `ClientBuilder`, so a client can name someone
to contact after failing to reach the homeserver at all.

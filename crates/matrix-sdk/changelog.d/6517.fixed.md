The `ThreadSummary` of a `ThreadEventCache` is computed by using the
`latest_events` internal API, making it understand more events and delivering a
more accurate result. This is still not perfect and complete but at least it's
one step in the right direction!

Defer room key bundle downloads on server-initiated joins (e.g. an auto-accepted knock, which lands on all of a user's devices at once): the bundle is only downloaded by the device the server designates via the new `org.matrix.msc4509.key_bundle_claim` to-device message, or lazily upon the first undecryptable event in the room. Client-initiated joins keep the eager behaviour.

Blocking a user no longer resets the latest event to `None` in every room.

Previously, ignoring or unignoring a user cleared the entire event cache, which
caused the latest event in every room to be reset to `None`, even in rooms with
no events from the blocked user. Now, events from ignored senders are filtered
out when computing the latest event, and the event cache is preserved.

Added `Client::subscribe_to_to_device_messages`, a stream of the custom to-device messages received by this client, 
optionally restricted to a list of event types , yielded as `ToDeviceMessage`s carrying the decrypted event and, 
for a message that arrived encrypted, its `EncryptionInfo`

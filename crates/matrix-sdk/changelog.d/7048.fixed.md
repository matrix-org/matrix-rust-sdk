Unread counts are now recomputed when a back-pagination loads a read receipt's
target event from the local store, not just when it is fetched from the network.
Previously a room kept a stale count until an unrelated sync recomputed it,
whether the target sat in a never-loaded chunk or in one unloaded after a
limited sync.

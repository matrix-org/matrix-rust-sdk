Add `RoomSendQueue::edit_with_attachment()`, which replaces the attachment of a message the current user sent, or adds one to a message which had none. The new attachment is uploaded through the send queue like `send_attachment()` does, then an `m.replace` edit carrying it is sent.

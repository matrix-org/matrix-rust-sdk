[**breaking**] `LoginWithQrCode`, `LoginWithGeneratedQrCode`, `GrantLoginWithScannedQrCode`
and `GrantLoginWithGeneratedQrCode` no longer borrow from the `Client` and QR code data, so
they can be moved into spawned tasks, e.g. when cancelling them cooperatively with
`CancellableIntoFutureExt::cancellable`.

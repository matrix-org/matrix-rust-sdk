Events written to the event cache store are no longer deserialised in full to strip their bundled relations. Only the `unsigned` field is read, and an event that carries `m.relations` is rebuilt around a new `unsigned` without parsing the rest. The stored events are unchanged.

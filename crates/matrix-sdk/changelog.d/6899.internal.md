Fix flakiness in the cross-process store lock tests. Dropping a guard releases the lock from the lease-renewal task, so the tests now poll for the acquisition instead of one attempt after a fixed sleep. Each run also gets its own temporary directory for the sqlite database, instead of a predictable shared path under the system temporary directory that carried state into the next run.

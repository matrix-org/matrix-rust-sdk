Added `Client::get_retention_configuration` to retrieve the server level 
retention policy and per room overrides so a client can determine whether
the server enforces any retention policy constraints.

Fix unpinning an event not updating the pinned events timeline through `pinned_event_listener_task`.

Adding a pinned event updated the list, clearing it also did, but removing one or several items from it, 
as long as it wasn't empty, didn't.
`ClientBuilder::with_x509_signer` and `ClientBuilder::with_x509_verifier`
now allow configuring an X.509 signer and verifier for experimental
X.509-based cross-signing identity verification.

Gated behind the `experimental-x509-identity-verification` feature.

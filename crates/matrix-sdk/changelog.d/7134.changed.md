The OAuth 2.0 login flows now request the stable `urn:matrix:client:api:*`
and `urn:matrix:client:device:<device_id>` scopes (introduced in Matrix v1.15),
instead of the unstable `urn:matrix:org.matrix.msc2967.client:*` variants.

The event cache gained a shared `BackPaginationQueue` (`EventCache::back_pagination_queue`): a single background executor that runs back-pagination requests from every use case by `Priority`, with a bounded in-flight number (`EventCacheConfig::max_concurrent_back_paginations`) and one run per room at a time. Enable it with `ClientBuilder::with_enable_automatic_back_pagination(true)`.

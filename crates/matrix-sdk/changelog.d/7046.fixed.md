Restore checksums for UniFFI functions. 

They were removed to work around an issue where the checksums were incorrectly computed first for some 
32bit ARM devices and then for some 64bit ones. That issue is now fixed so we should restore the checksums.


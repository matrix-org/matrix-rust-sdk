The inlined HTML script that extracts and renders Mermaid diagrams in the rustdoc has been included in the published crate source. Previously they were not, and the script was referenced in the rustdoc with a path relative to the monorepo root, which failed to resolve when individual crates are published, thus breaking rustdoc build on docs.rs.

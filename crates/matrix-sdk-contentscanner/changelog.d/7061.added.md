Implemented `ContentScanner::get_media_content_with_config` to match the new function in the `MediaFetcher` trait. 
With this, you can retrieve the content of some `MediaSource` providing a custom `RequestConfig` to handle retries 
or timeouts.
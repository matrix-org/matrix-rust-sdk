Added a new index to the `events` object store of the `EventCacheStore` implementation
for tracking whether an `Event` is out-of-band or not. This provides a quick way to check
whether a given `Event` has been stored out-of-band without having to deserialize the
entire `Event` from the database.

`PRAGMA cache_size` and `PRAGMA journal_size_limit` are per-connection settings,
but they were applied to a single connection when a store was opened. The
connection pool manager now applies them to every connection it creates.

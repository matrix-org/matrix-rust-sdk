A notification whose push actions say it shouldn't notify is now consistently filtered out by
`NotificationClient`, where it could previously be returned because its push actions couldn't be
computed.

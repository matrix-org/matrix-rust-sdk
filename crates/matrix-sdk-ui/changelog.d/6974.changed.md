[**breaking**] `ReactionStatus` is gone: `ReactionInfo::status` is replaced by `ReactionInfo::send_state`, an `Option<EventSendState>` that is `None` for reactions received from the server. Every aggregation (reactions, edits, redactions) now carries the same send state, and failed, retried and uploading aggregations are reflected on their timeline item instead of being ignored.

Load the latest reply of a thread root only once when building its timeline item. It was previously loaded twice, once for the embedded reply preview and once for the implicit read receipt check, which cost an extra event cache lookup, or an extra `/event` request when the reply was not cached, for every thread root in the timeline.

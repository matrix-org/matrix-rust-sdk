Add `RoomListService::remove_room_subscriptions` and
`RoomListService::reset_and_add_room_subscriptions`, forwarding the room
subscription methods of `SlidingSync` with the room list settings. The former
removes subscriptions, the latter replaces the whole subscription set and marks
the members of the new rooms as missing.

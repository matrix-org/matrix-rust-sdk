[**breaking**] Reactions move from `TimelineItemContent::reactions()` back to
`EventTimelineItem::reactions()`, returning a reference instead of an `Option`.
Only the `MsgLike` variant could hold them, so an annotation on a membership
change, a profile change, another state event, a call invite or an unparsable
event was dropped. MSC2677 makes any event type eligible for an annotation.

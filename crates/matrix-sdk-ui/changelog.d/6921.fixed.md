A pending edit now follows the send queue's updates to its content, so an
edit replacing an attachment points at the uploaded media as soon as the
upload is done, instead of a local cache entry which doesn't exist anymore.

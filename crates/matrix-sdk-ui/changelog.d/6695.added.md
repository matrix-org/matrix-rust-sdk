Add a reactive `search::SearchService` that aggregates results of different kinds (currently only messages) into a single list of typed `SearchResult`s, observed as a stream of `VectorDiff`s. Gated behind the new `experimental-search` feature.

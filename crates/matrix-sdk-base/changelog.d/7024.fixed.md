Empty room account data entries in a sliding sync response no longer produce a
`JoinedRoomUpdate`/`LeftRoomUpdate` for their room, so a response with a single
room update no longer triggers an event cache write for every room in the list.

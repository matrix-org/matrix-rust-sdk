Add integration test to ensure all implementations of `EventCacheStore` filter
incomplete `Event`s rather than failing immediately when handling `Update::PushItems`
and `Update::ReplaceItem`. Additionally, update `MemoryStore` so that it is compliant
with this behavior.

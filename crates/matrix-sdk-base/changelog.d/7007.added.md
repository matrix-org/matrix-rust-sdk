[**breaking**] `StateStore` has a new required method, `get_event_room_receipt_events_batch`, which reads the read receipts of several events in one call and returns them keyed by event id. Custom `StateStore` implementations must implement it. `Room::load_event_receipts_batch` is added on top of it.
